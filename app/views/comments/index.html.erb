<p>As of <%= Date.today %>, there are 
<%= @comments.count %> reviews.

<p>The last review was 

  <%= @comments.most_recently_updated { |comment| comment.title } %>
  (FIx this to display correctly)
    
