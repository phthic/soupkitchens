<h2>New comment for <%= @soupkitchen.name %> </h2>



<%= form_for( @soupkitchen, @comments) do |f| %>


<h4>(You must be logged in to add a comment.)</h4>
	<%= f.hidden_field :soupkitchen_id, value: @soupkitchen.id %>
	


	<%= f.label "Title of Comment" %><br>
	<%= f.text_field :title %>
	<br>
	<%= f.label "Comment" %><br>
	<%= f.text_area :content, cols: 40, rows: 15 %>
	<br>
	Add Rating (from 1 - 5)
	<br>
		


	<%= f.submit %>
<% end %>



  user[:first_name]
  <p>

Move to form -- 
form_with(model: 